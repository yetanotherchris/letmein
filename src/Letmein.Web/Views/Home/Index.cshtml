@{
	ViewData["Title"] = "Home Page";
}

<div class="row">
	<div class="col-md-12 encrypt-containers">
		
		<p>
			Enter the text you want to encrypt, and create a password to encrypt it with.	
		</p>

		<form method="post" action="/Store" id="encrypt-form">
			<input type="hidden" id="cipherJson" name="cipherJson" />
			<textarea name="text" id="text-input" rows="10" placeholder="Enter your text to encrypt"></textarea>
			
			<div class="row">
				<div class="col-md-10">
					<input name="password" id="password-input" class="password" type="password" placeholder="Create a password" />
				</div>
				<div class="col-md-2">
					<input type="submit" id="submit-button" class="btn btn-success" value="Encrypt" />
				</div>
			</div>
		</form>
	</div>
</div>

@section scripts
{
	<script>
		$(document).ready(function () {
			$("#text-input").focus();

			$("#encrypt-form").submit(function () {
				var text = $("#text-input").val();
				var password = $("#password-input").val();

				// Clear so they're not POST'd
				$("#text-input").val("");
				$("#password-input").val("");

				// Encrypt
				var json = window.sjcl.encrypt(password, text);
				$("#cipherJson").val(json);

				return true;
			});
		});
	</script>
}