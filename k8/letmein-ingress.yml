apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: letmein-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: letmein-service
            port:
              number: 80
            
